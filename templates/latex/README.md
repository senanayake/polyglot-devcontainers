# LaTeX Template

This template consumes the `polyglot-devcontainers-latex` image.

It provides a small IEEE-style document that exercises `IEEEtran`, BibTeX,
`booktabs`, `makecell`, and TikZ.

```bash
task ci
```
