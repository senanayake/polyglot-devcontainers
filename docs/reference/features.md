# Features

## security-baseline

Purpose:

- install `task`
- install `gitleaks`

## python-engineering

Purpose:

- install `pre-commit`
- install `uv`

## node-engineering

Purpose:

- enable `corepack`
- activate a pinned `pnpm`

## agent-runtime

Purpose:

- install `jq`
- install `ripgrep`
- install `fd-find`
- install `shellcheck`
