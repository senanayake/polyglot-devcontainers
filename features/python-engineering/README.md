# python-engineering

Adds lightweight Python workflow helpers to a devcontainer:

- `pre-commit`
- `uv`

Project-specific lint, test, and scan dependencies should still be installed in
the project environment rather than globally.
