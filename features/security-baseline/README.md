# security-baseline

Installs the core workflow and security tools shared across repository
templates:

- `task`
- `gitleaks`

Use this feature when you need the standard repository task contract and secret
scanning inside a devcontainer.
