# java-engineering

Configures a Java devcontainer for a Gradle-first workflow.

The feature assumes Java is already present and:

- installs pinned `gradle`
- installs pinned `trivy`
- keeps the Java template aligned with the repository task contract
