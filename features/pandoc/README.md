# pandoc

Adds deterministic document rendering tools to a devcontainer:

- `pandoc`
- optional XeLaTeX support via `texlive-latex-recommended` and `texlive-xetex`

Enable the `texlive` option only when the workspace needs PDF output. HTML and
other non-LaTeX document conversions only require `pandoc`.
